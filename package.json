{
  "name": "libraryproject",
  "version": "1.0.0",
  "description": "A management software for a library of books which will use apis to look up books. Allows users to checkout books.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "Apache-2.0",
  "dependencies": {
    "jsonwebtoken": "^8.2.0",
    "node-isbn": "^1.3.0",
    "sodium": "^2.0.3"
  },
  "devDependencies": {
    "eslint": "^4.19.1"
  }
}
